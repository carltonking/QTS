{
  "id": 2249,
  "title": "Count Lattice Points Inside a Circle",
  "slug": "count-lattice-points-inside-a-circle",
  "difficulty": "Medium",
  "tags": [],
  "description": "Given a 2D integer array circles where circles[i] = [xi, yi, ri] represents the center (xi, yi) and radius ri of the ith circle drawn on a grid, return _the number of lattice points_ _that are present inside at least one circle_.\n\nNote:\n\n*   A lattice point is a point with integer coordinates.\n*   Points that lie on the circumference of a circle are also considered to be inside it.",
  "examples": [],
  "constraints": [],
  "starterCode": {
    "python": "",
    "javascript": "",
    "java": "",
    "cpp": ""
  },
  "solutionCode": {
    "python": "\n    ```python\ndef count_sequences(differences, lower, upper):\n    count = 0\n    n = len(differences)\n    hidden = [0] * (n + 1)\n\n    hidden[0] = lower\n    while True:\n        valid = True\n        for i in range(1, n + 1):\n            hidden[i] = hidden[i - 1] + differences[i - 1]\n            if hidden[i] < lower or hidden[i] > upper:\n                valid = False\n                break\n        if valid: count += 1\n\n        if hidden[0] == upper: break\n        hidden[0] += 1\n    \n    return count\n```\n\n    \n    We initialize a `count` variable to keep track of the number of valid hidden sequences found so far, and an `n` variable to store the length of the `differences` array. Then, we create a `hidden` array, initialized with the `lower` value as the first element, and run a while loop until the first element of `hidden` is equal to the `upper` value.\n\nInside the loop, we set a `valid` variable to true, and iterate through each index `i` of length `n + 1`. At each iteration, we calculate the difference between consecutive hidden sequence elements using `differences[i - 1]`. If the resulting value is outside the range `[lower, upper]`, set `valid` to false and break the loop.\n\nAfter iterating through all elements, if `valid` is still true, increment the `count` variable. At the end of the while loop, increment the value of the first element of `hidden`. The loop will continue until the first element of `hidden` is equal to the `upper` value. Finally, return the `count` variable, which holds the number of possible hidden sequences.\n    ",
    "javascript": "\n    ```javascript\nfunction countSequences(differences, lower, upper) {\n    let count = 0, n = differences.length;\n    let hidden = new Array(n + 1);\n\n    hidden[0] = lower;\n    while (true) {\n        let valid = true;\n        for (let i = 1; i <= n; i++) {\n            hidden[i] = hidden[i - 1] + differences[i - 1];\n            if (hidden[i] < lower || hidden[i] > upper) {\n                valid = false;\n                break;\n            }\n        }\n        if (valid) count++;\n\n        if (hidden[0] === upper) break;\n        hidden[0]++;\n    }\n    return count;\n}\n```\n\n    \n    We initialize a `count` variable to keep track of the number of valid hidden sequences found so far, and an `n` variable to store the length of the `differences` array. Then, we create a `hidden` array, initialized with the `lower` value as the first element, and run a while loop until the first element of `hidden` is equal to the `upper` value.\n\nInside the loop, we set a `valid` variable to true, and iterate through each index `i` of length `n + 1`. At each iteration, we calculate the difference between consecutive hidden sequence elements using `differences[i - 1]`. If the resulting value is outside the range `[lower, upper]`, set `valid` to false and break the loop.\n\nAfter iterating through all elements, if `valid` is still true, increment the `count` variable. At the end of the while loop, increment the value of the first element of `hidden`. The loop will continue until the first element of `hidden` is equal to the `upper` value. Finally, return the `count` variable, which holds the number of possible hidden sequences.\n    ",
    "java": "\n    ```java\nint countSequences(int[] differences, int lower, int upper) {\n    int count = 0, n = differences.length;\n    int[] hidden = new int[n + 1];\n\n    hidden[0] = lower;\n    while (true) {\n        boolean valid = true;\n        for (int i = 1; i <= n; i++) {\n            hidden[i] = hidden[i - 1] + differences[i - 1];\n            if (hidden[i] < lower || hidden[i] > upper) {\n                valid = false;\n                break;\n            }\n        }\n        if (valid) count++;\n\n        if (hidden[0] == upper) break;\n        hidden[0]++;\n    }\n    return count;\n}\n```\n\n    \n    We initialize a `count` variable to keep track of the number of valid hidden sequences found so far, and an `n` variable to store the length of the `differences` array. Then, we create a `hidden` array, initialized with the `lower` value as the first element, and run a while loop until the first element of `hidden` is equal to the `upper` value.\n\nInside the loop, we set a `valid` variable to true, and iterate through each index `i` of length `n + 1`. At each iteration, we calculate the difference between consecutive hidden sequence elements using `differences[i - 1]`. If the resulting value is outside the range `[lower, upper]`, set `valid` to false and break the loop.\n\nAfter iterating through all elements, if `valid` is still true, increment the `count` variable. At the end of the while loop, increment the value of the first element of `hidden`. The loop will continue until the first element of `hidden` is equal to the `upper` value. Finally, return the `count` variable, which holds the number of possible hidden sequences.\n    ",
    "cpp": ""
  },
  "testCases": [],
  "acceptanceRate": 0
}

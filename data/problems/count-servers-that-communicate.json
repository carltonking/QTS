{
  "id": 1267,
  "title": "Count Servers that Communicate",
  "slug": "count-servers-that-communicate",
  "difficulty": "Medium",
  "tags": [],
  "description": "You are given a map of a server center, represented as a m * n integer matrix grid, where 1 means that on that cell there is a server and 0 means that it is no server. Two servers are said to communicate if they are on the same row or on the same column.\n\nReturn the number of servers that communicate with any other server.",
  "examples": [],
  "constraints": [],
  "starterCode": {
    "python": "",
    "javascript": "",
    "java": "",
    "cpp": ""
  },
  "solutionCode": {
    "python": "\n    ```python\ndef remove_zero_sum_sublists(head):\n    dummy = ListNode(0)\n    dummy.next = head\n    current = dummy\n\n    while current:\n        sum = 0\n        while head:\n            sum += head.val\n            if sum == 0:\n                current.next = head.next\n            head = head.next\n        current = current.next\n        if current:\n            head = current.next\n\n    return dummy.next\n```\n\n    \n    We create a dummy node as the initial pointer to keep track of the head of the list. We initialize two pointers, current and head, to the dummy node.\n\nWe loop through the linked list while current is not a null pointer. Inside the loop, we also loop through the list while head is not a null pointer. We maintain a sum variable to calculate the accumulated sum of the nodes we traverse. If we encounter a node such that the accumulated sum is zero, we update the next pointer of the current node to the next pointer of head.\n\nOutside the inner loop, we assign current to its next pointer and head to the next pointer of current. We repeat this process until we reach the end of the linked list.\n\nFinally, we return the next pointer of the dummy node, which now represents the head of the linked list.\n    ",
    "javascript": "\n    ```javascript\nfunction removeZeroSumSublists(head) {\n    const dummy = new ListNode(0);\n    dummy.next = head;\n    let current = dummy;\n\n    while (current) {\n        let sum = 0;\n        while (head) {\n            sum += head.val;\n            if (sum === 0) {\n                current.next = head.next;\n            }\n            head = head.next;\n        }\n        current = current.next;\n        if (current) {\n            head = current.next;\n        }\n    }\n\n    return dummy.next;\n}\n```\n\n    \n    We create a dummy node as the initial pointer to keep track of the head of the list. We initialize two pointers, current and head, to the dummy node.\n\nWe loop through the linked list while current is not a null pointer. Inside the loop, we also loop through the list while head is not a null pointer. We maintain a sum variable to calculate the accumulated sum of the nodes we traverse. If we encounter a node such that the accumulated sum is zero, we update the next pointer of the current node to the next pointer of head.\n\nOutside the inner loop, we assign current to its next pointer and head to the next pointer of current. We repeat this process until we reach the end of the linked list.\n\nFinally, we return the next pointer of the dummy node, which now represents the head of the linked list.\n    ",
    "java": "\n    ```java\npublic ListNode removeZeroSumSublists(ListNode head) {\n    ListNode dummy = new ListNode(0);\n    dummy.next = head;\n    ListNode current = dummy;\n\n    while (current != null) {\n        int sum = 0;\n        while (head != null) {\n            sum += head.val;\n            if (sum == 0) {\n                current.next = head.next;\n            }\n            head = head.next;\n        }\n        current = current.next;\n        if (current != null) {\n            head = current.next;\n        }\n    }\n\n    return dummy.next;\n}\n```\n\n    \n    We create a dummy node as the initial pointer to keep track of the head of the list. We initialize two pointers, current and head, to the dummy node.\n\nWe loop through the linked list while current is not a null pointer. Inside the loop, we also loop through the list while head is not a null pointer. We maintain a sum variable to calculate the accumulated sum of the nodes we traverse. If we encounter a node such that the accumulated sum is zero, we update the next pointer of the current node to the next pointer of head.\n\nOutside the inner loop, we assign current to its next pointer and head to the next pointer of current. We repeat this process until we reach the end of the linked list.\n\nFinally, we return the next pointer of the dummy node, which now represents the head of the linked list.\n    ",
    "cpp": ""
  },
  "testCases": [],
  "acceptanceRate": 0
}

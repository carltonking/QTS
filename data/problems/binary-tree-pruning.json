{
  "id": 814,
  "title": "Binary Tree Pruning",
  "slug": "binary-tree-pruning",
  "difficulty": "Medium",
  "tags": [],
  "description": "Given the root of a binary tree, return _the same tree where every subtree (of the given tree) not containing a_ 1 _has been removed_.\n\nA subtree of a node node is node plus every node that is a descendant of node.",
  "examples": [],
  "constraints": [],
  "starterCode": {
    "python": "",
    "javascript": "",
    "java": "",
    "cpp": ""
  },
  "solutionCode": {
    "python": "\n    ```python\ndef best_rotation(nums):\n    n = len(nums)\n    change = [0] * n\n\n    for i in range(n):\n        change[(i - nums[i] + 1 + n) % n] -= 1\n        change[(i + 1) % n] += 1\n\n    max_score = 0\n    cur_score = change[0]\n    for i in range(1, n):\n        cur_score += change[i]\n        if cur_score > max_score:\n            max_score = cur_score\n            \n    return max_score\n```\n\n    \n    The idea is to calculate the change in score for each rotation instead of recalculating the score every time. We create a `change` array to store the difference in scores when the array is rotated by 1.\n\nFor each index `i` in `nums`, decrement the change array value at `(i - nums[i] + 1 + n) % n` by 1 and increment the change array value at `(i + 1) % n` by 1. This will maintain the difference in scores as we perform the rotations.\n\nAfter that, loop from `i = 1` to `i = n`, updating the `cur_score` by adding the value at `change[i]`. Track the maximum score during the loop.\n\nFinally, return the maximum score.\n    ",
    "javascript": "\n    ```javascript\nfunction best_rotation(nums) {\n    const n = nums.length;\n    const change = new Array(n).fill(0);\n\n    for (let i = 0; i < n; i++) {\n        change[(i - nums[i] + 1 + n) % n] -= 1;\n        change[(i + 1) % n] += 1;\n    }\n\n    let max_score = 0;\n    for (let i = 1, cur_score = change[0]; i < n; i++) {\n        cur_score += change[i];\n        if (cur_score > max_score) {\n            max_score = cur_score;\n        }\n    }\n\n    return max_score;\n}\n```\n\n    \n    The idea is to calculate the change in score for each rotation instead of recalculating the score every time. We create a `change` array to store the difference in scores when the array is rotated by 1.\n\nFor each index `i` in `nums`, decrement the change array value at `(i - nums[i] + 1 + n) % n` by 1 and increment the change array value at `(i + 1) % n` by 1. This will maintain the difference in scores as we perform the rotations.\n\nAfter that, loop from `i = 1` to `i = n`, updating the `cur_score` by adding the value at `change[i]`. Track the maximum score during the loop.\n\nFinally, return the maximum score.\n    ",
    "java": "\n    ```java\npublic int best_rotation(int[] nums) {\n    int n = nums.length;\n    int[] change = new int[n];\n\n    for (int i = 0; i < n; i++) {\n        change[(i - nums[i] + 1 + n) % n] -= 1;\n        change[(i + 1) % n] += 1;\n    }\n\n    int max_score = 0;\n    for (int i = 1, cur_score = change[0]; i < n; i++) {\n        cur_score += change[i];\n        if (cur_score > max_score) {\n            max_score = cur_score;\n        }\n    }\n    return max_score;\n}\n```\n\n    \n    The idea is to calculate the change in score for each rotation instead of recalculating the score every time. We create a `change` array to store the difference in scores when the array is rotated by 1.\n\nFor each index `i` in `nums`, decrement the change array value at `(i - nums[i] + 1 + n) % n` by 1 and increment the change array value at `(i + 1) % n` by 1. This will maintain the difference in scores as we perform the rotations.\n\nAfter that, loop from `i = 1` to `i = n`, updating the `cur_score` by adding the value at `change[i]`. Track the maximum score during the loop.\n\nFinally, return the maximum score.\n    ",
    "cpp": ""
  },
  "testCases": [],
  "acceptanceRate": 0
}

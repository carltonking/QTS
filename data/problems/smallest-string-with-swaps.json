{
  "id": 1202,
  "title": "Smallest String With Swaps",
  "slug": "smallest-string-with-swaps",
  "difficulty": "Medium",
  "tags": [],
  "description": "You are given a string s, and an array of pairs of indices in the string pairs where pairs[i] = [a, b] indicates 2 indices(0-indexed) of the string.\n\nYou can swap the characters at any pair of indices in the given pairs any number of times.\n\nReturn the lexicographically smallest string that s can be changed to after using the swaps.",
  "examples": [],
  "constraints": [],
  "starterCode": {
    "python": "",
    "javascript": "",
    "java": "",
    "cpp": ""
  },
  "solutionCode": {
    "python": "\n    ```python\nfrom collections import defaultdict\n\nclass Solution:\n    def smallestStringWithSwaps(self, s: str, pairs: List[List[int]]) -> str:\n        n = len(s)\n\n        # Initialize the parents array\n        p = list(range(n))\n\n        # Helper function to find the parent of an index\n        def find(x):\n            if p[x] != x:\n                p[x] = find(p[x])\n            return p[x]\n\n        # Union-find, grouping connected indices together\n        for pair in pairs:\n            root1 = find(pair[0])\n            root2 = find(pair[1])\n            if root1 != root2:\n                p[root2] = root1\n                \n        # Group all the characters together according to their representatives (parent)\n        groups = defaultdict(list)\n        for i in range(n):\n            root = find(i)\n            groups[root].append(s[i])\n\n        # Sort each group\n        for group in groups.values():\n            group.sort(reverse=True)\n\n        # Build the result string\n        res = []\n        for i in range(n):\n            root = find(i)\n            res.append(groups[root].pop())\n\n        return \"\".join(res)\n```\n\n    \n    The problem can be solved using the Union-Find data structure. First, group the connected indices of the string, and the characters at these connected indices can be freely swapped. We can, therefore, sort the characters in the connected groups lexicographically. Lastly, build the smallest string by taking the next smallest character from each group.\n\n1. Initialize the Union-Find data structure and perform the union in the find operation for each pair of indices.\n2. Find the representative/parent index for each character and group the characters together based on their parent index.\n3. Sort each group in decreasing lexicographical order.\n4. Finally, iterate through each character position in the input string and pop the smallest character from each group to construct the result string.\n\nThe output string will be the lexicographically smallest string that can be constructed using the given swaps.\n    ",
    "javascript": "\n    ```javascript\n/**\n * @param {string} s\n * @param {number[][]} pairs\n * @return {string}\n */\nconst smallestStringWithSwaps = (s, pairs) => {\n    const n = s.length;\n    const p = Array.from({length: n}, (_, i) => i);\n    \n    const find = x => {\n        if (p[x] !== x) p[x] = find(p[x]);\n        return p[x];\n    };\n\n    for (const [x, y] of pairs) {\n        p[find(y)] = find(x);\n    }\n\n    const groups = Array.from({length: n}, () => []);\n    for (let i = 0; i < n; i++) {\n        const root = find(i);\n        groups[root].push(s[i]);\n    }\n    \n    for (const group of groups) {\n        group.sort((a, b) => a.charCodeAt(0) - b.charCodeAt(0));\n    }\n    \n    let result = '';\n    for (let i = 0; i < n; i++) {\n        const root = find(i);\n        result += groups[root].pop();\n    }\n\n    return result;\n};\n```\n\n    \n    The problem can be solved using the Union-Find data structure. First, group the connected indices of the string, and the characters at these connected indices can be freely swapped. We can, therefore, sort the characters in the connected groups lexicographically. Lastly, build the smallest string by taking the next smallest character from each group.\n\n1. Initialize the Union-Find data structure and perform the union in the find operation for each pair of indices.\n2. Find the representative/parent index for each character and group the characters together based on their parent index.\n3. Sort each group in decreasing lexicographical order.\n4. Finally, iterate through each character position in the input string and pop the smallest character from each group to construct the result string.\n\nThe output string will be the lexicographically smallest string that can be constructed using the given swaps.\n    ",
    "java": "\n    ```java\nimport java.util.*;\n\nclass Solution {\n    public String smallestStringWithSwaps(String s, int[][] pairs) {\n        int n = s.length();\n        int[] p = new int[n];\n\n        for (int i = 0; i < n; i++) {\n            p[i] = i;\n        }\n\n        for (int[] pair : pairs) {\n            int root1 = find(pair[0], p);\n            int root2 = find(pair[1], p);\n            if (root1 != root2) {\n                p[root2] = root1;\n            }\n        }\n\n        Map<Integer, PriorityQueue<Character>> groups = new HashMap<>();\n        for (int i = 0; i < n; i++) {\n            int root = find(i, p);\n            groups.computeIfAbsent(root, k -> new PriorityQueue<>()).add(s.charAt(i));\n        }\n\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < n; i++) {\n            int root = find(i, p);\n            sb.append(groups.get(root).poll());\n        }\n\n        return sb.toString();\n    }\n\n    private int find(int x, int[] p) {\n        if (p[x] != x) {\n            p[x] = find(p[x], p);\n        }\n\n        return p[x];\n    }\n}\n```\n\n    \n    The problem can be solved using the Union-Find data structure. First, group the connected indices of the string, and the characters at these connected indices can be freely swapped. We can, therefore, sort the characters in the connected groups lexicographically. Lastly, build the smallest string by taking the next smallest character from each group.\n\n1. Initialize the Union-Find data structure and perform the union in the find operation for each pair of indices.\n2. Find the representative/parent index for each character and group the characters together based on their parent index.\n3. Sort each group in decreasing lexicographical order.\n4. Finally, iterate through each character position in the input string and pop the smallest character from each group to construct the result string.\n\nThe output string will be the lexicographically smallest string that can be constructed using the given swaps.\n    ",
    "cpp": ""
  },
  "testCases": [],
  "acceptanceRate": 0
}
